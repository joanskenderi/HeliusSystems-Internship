using System; // A namespace created by C#

namespace HelloWorld // Creates a namespace called HelloWorld
{
    class Program // Creates a class called Program
    {
        static void Main(string[] args) // Method that accepts string as arguments 
        {
            Console.WriteLine("Hello World"); // Method that prints the string inside the ()

            Console.Read(); // Used to keep the Commant Prompt open
        }
    }
}
